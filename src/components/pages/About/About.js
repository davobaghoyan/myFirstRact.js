import React from 'react';

export default function About(props){

    return(
        <div>
        <h5>This is a simply project created during my React.js course. This is a ToDo application, where you can add your tasks,complete them,delete and so on.</h5>
        <h1 style={{color: "red"}}>Don't judge strictly!</h1>
</div>
    );
};